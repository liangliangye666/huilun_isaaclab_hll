# 核心目录结构
huilun_isaaclab/
│
├── source/huilun_isaaclab/          # ← 训练核心代码
│   ├── assets/robots/l5a.py         # 机器人资产定义（URDF路径、关节名、初始状态、PD参数）
│   ├── actuators/                    # 自定义执行器（带延迟的隐式执行器）
│   ├── tasks/locomotion/l5a/        # 训练任务
│   │   ├── wf_flat_env_cfg.py       # ★ 环境配置（最核心文件）
│   │   └── mdp/                     # 各模块实现
│   │       ├── observations.py      # 观测函数（策略/历史/critic）
│   │       ├── rewards.py           # 奖励函数
│   │       ├── actions.py           # 动作解析
│   │       ├── events.py            # 域随机化事件
│   │       └── terminations.py      # 终止条件
│   └── learning/rsl_rl/            # RSL-RL 训练适配层
│       ├── estimator_actor_critic.py # Encoder + Actor + Critic 网络
│       ├── velocity_estimator_cfg.py # 训练配置
│       └── estimator_exporter.py    # ONNX 导出
│
├── resources/robots/l5a/           # 机器人模型资源
│   ├── urdf/l5aurdf20260521.urdf   # URDF（关节定义、质量、惯量）
│   ├── usd/                         # Isaac Sim 用的 USD 格式
│   ├── xml/                         # MuJoCo 用的 XML 格式
│   └── meshes/                      # STL 网格文件
│
├── sim2sim_mujoco_py/              # Python 版 MuJoCo sim2sim 验证，支持固定指令和键盘控制机体（暂不参考）
│
├── origin_code/                    # 基于原始参考代码适配
│   ├── sim2sim_cpp_and_sim2real/   # C++ sim2sim + 真机部署代码
│   └── sim2sim_mujoco_py/          # Python sim2sim + 真机部署代码
│
├── logs/rsl_rl/                    # 训练日志和模型
│
└── scripts/                        # 工具脚本
│   └── rsl_rl/
│       ├── train.py                # 训练入口
│       └── play.py                 # 评估/导出入口

